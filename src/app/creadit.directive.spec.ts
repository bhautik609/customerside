import { CreaditDirective } from './creadit.directive';

describe('CreaditDirective', () => {
  it('should create an instance', () => {
    const directive = new CreaditDirective();
    expect(directive).toBeTruthy();
  });
});
